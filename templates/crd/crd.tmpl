{{ define "crd" }}

# {{ .Name }}

{{ range .Objects "alphabetical" }}

## <a id="{{ .Name }}">{{ .Name }}</a>

{{ .Description | unwrap }}

{{ if .Uses -}}
    {{- if .Uses | len | eq 1 -}}
        {{- with index .Uses 0 }}
Used by [{{ .Name }}](#{{ .Name }})
        {{- end -}}
    {{- else }}
Used by:
        {{- range .Uses }}
* [{{ .Name }}](#{{ .Name }})
        {{- end -}}
    {{- end -}}
{{ end }}

| Name |  Description | Type |
| -- | -- | -- |
{{ range .Properties -}}
| {{ .Name -}} 
| {{ .Description | unwrap -}} 
| {{ if .Type | asId -}}
    [
  {{- end -}}
  {{- .Type | asText -}}
  {{- if .Type | asId -}}
    ](#{{ .Type | asId }})
  {{- end -}}
  {{ if .Required -}}
    <br/>{{ .Required -}}
  {{ end -}} |
{{ end }}

{{ end }}

{{ end }}
