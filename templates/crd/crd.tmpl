{{ define "crd" }}

# {{ .Name }}

{{ range .Objects "alphabetical" }}

## <a id="{{ .Name }}">{{ .Name }}</a>

{{ .Description | unwrap }}

| Name |  Description | Type |
| -- | -- | -- |
{{ range .Properties -}}
| {{ .Name -}} 
| {{ .Description | unwrap -}} 
| {{ if .Type | asId -}}
    [
  {{- end -}}
  {{- .Type | asText -}}
  {{- if .Type | asId -}}
    ](#{{ .Type | asId }})
  {{- end -}}
  {{ if .Required -}}
    <br/>{{ .Required -}}
  {{ end -}} |
{{ end }}

{{ end }}

{{ end }}
